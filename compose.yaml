services:
  campaignhub:
    build: .
    image: campaignhub:latest
    container_name: campaignhub
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      JWT_SECRET: campaignhub-jwt-secret-key-change-in-prod-32bytes-min
    restart: unless-stopped
